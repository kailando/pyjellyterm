[project]
name = "jellyfin-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kailando", email = "dq11sk@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
jellyfin-client = "jellyfin_client:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
