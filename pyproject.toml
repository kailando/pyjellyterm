[project]
name = "jellyfin-client"
version = "0.1.0"
description = "A Jellyfin terminal client written in Python"
authors = [
    { name = "kailando", email = "dq11sk@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
jellyfin-client = "python main.py"

[tool.uv]
package = false